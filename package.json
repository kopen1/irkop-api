{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "description": "Gateway API untuk seluruh project IRKOP",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
